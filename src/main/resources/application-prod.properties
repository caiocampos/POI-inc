# Perfil de producao - MongoDB Atlas
# As credenciais SEMPRE vem de variaveis de ambiente (mongoUser / mongoUserP),
# nunca devem ser commitadas em texto plano.
spring.data.mongodb.uri=mongodb+srv://${mongoUser}:${mongoUserP}@clusterpoi-4ep6n.gcp.mongodb.net/poi?retryWrites=true&w=majority
spring.data.mongodb.database=poi
